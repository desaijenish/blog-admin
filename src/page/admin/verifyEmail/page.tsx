import * as React from "react";
import { Layout } from "../../../components/layout";
import { VerifyEmailPage } from "./sing-up-form";

export default function VerifyEmail(): React.JSX.Element {
  return (
    <Layout>
      <VerifyEmailPage />
    </Layout>
  );
}
